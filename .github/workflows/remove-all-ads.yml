name: Remove all site ads
on:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled — MangaAtlas ads are managed by analytics.js"
